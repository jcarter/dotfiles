if status is-interactive
    # fish selects [light] or [dark] from the Dracula theme based on
    # fish_terminal_color_theme, which Ghostty keeps updated via OSC 11.
    fish_config theme choose Dracula
end
